# This file has been auto-generated by i3-config-wizard().
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!



# shadow_blur_radius 10
# default_dim_inactive 0.1
# for_window [app_id="vivaldi-stable"] dim_inactive 0.00
# for_window [app_id="firefoxdeveloperedition"] dim_inactive 0.00
# layer_effects "waybar" blur enable; shadows enable; corner_radius 6

# titlebar_separator disable

# set $opacity 0.7
set $opacity 0.7
# for_window [class=".*"] opacity $opacity
# for_window [app_id="okular"] opacity 0.6




exec_always "export PULSE_LATENCY_MSEC=83"
exec_always "export PIPEWIRE_LATENCY=1024/48000"
exec_always "export TERMINAL=wezterm"
exec_always "export BROWSER=/usr/bin/vivaldi"
# Important startup stuff
# exec_always "export WLR_NO_HARDWARE_CURSORS=1"
# exec_always "export QT_QPA_PLATFORM=wayland;xcb"
exec_always "export QT_QPA_PLATFORMTHEME=qt5ct"
exec_always "export XDG_CURRENT_DESKTOP=i3wm"
# exec_always 'gsettings set org.gnome.desktop.interface gtk-theme "Fantasma-GTK"'
# exec_always 'gsettings set org.gnome.desktop.interface icon-theme "Fantasma-GTK"'
# exec_always 'gsettings set org.gnome.desktop.interface gtk-theme "Midnight-Green"'
# exec_always 'gsettings set org.gnome.desktop.interface icon-theme "Midnight-Green"'
exec_always 'gsettings set org.gnome.desktop.interface cursor-theme Catppuccin Macchiato Lavender'
exec_always 'gsettings set org.gnome.desktop.interface gtk-theme "Dracula"'
exec_always 'export GTK_THEME=Dracula'

exec_always 'gsettings set org.gnome.desktop.interface gtk-key-theme "Emacs"'
# exec_always 'gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"'
# exec dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK
# exec --no-startup-id "~/bin/laptop.sh"
exec_always setxkbmap -option parens:swap_brackets
# exec --no-startup-id xcape -e 'Caps_Lock=Escape'
# exec --no-startup-id xmodmap ~/.xmodmap
# ctrl:swapcaps
# -option caps:ctrl_modifier -option terminate:ctrl_alt_bksp
# exec_always xcape -e 'Ctrl_L=Escape'
# -option ctrl:swapcaps
exec_always picom -cb --config ~/.config/picom/picom.conf

# Systemd services
exec "systemctl start bluetooth"
exec "systemctl start --user emacs && eclient"
exec "systemctl start --user cronie"
exec "systemctl start --user spotify-listener"
exec "systemctl start --user onedrive"
exec "systemctl start --user mopidy"
exec "systemctl start --user 5e-api"
exec "systemctl start --user syncthing"
exec "~/bin/eclient"



# input * {
#       xkb_options parens:swap_brackets
# }

# output HDMI-A-1 pos 0 0 res 1920x1080
# output eDP-1 pos 3840 1  res 1920x1200
# exec_always xrandr --output HDMI-A-1 --primary
# workspace 1 output HDMI-A-1


# -option compose:rctrl
set $mod Mod4
set $refresh_i3status killall -SIGUSR1 i3status

# Gap setup
# new_window pixel 0
# default_border pixel 0
# new_float none
# default_floating_none border
default_border pixel 1
# default_border pixel 1

# border pixel 5
gaps inner 4
gaps outer 10
smart_borders on
smart_gaps off

# # class                 border  bground text    indicator child_border
# client.focused #85b300 #987ac4 #6c71c4

# # class                 border  bground text    indicator child_border
# # client.focused #0088CC #0088CC #ffffff #dddddd


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).

font pango:Roboto 10
for_window [all] title_format "<b><i>%title</i></b>"
for_window [all] title_window_icon on
# font pango:Foglihten 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock -- i3lock -n -i  "/home/vivien/Pictures/backgrounds/rosepine/rose_pine_maze.png"  &
exec --no-startup-id xautolock -time 10 -locker 'i3lock --nofork -i /home/vivien/Pictures/backgrounds/rosepine/rose_pine_maze.png'
# exec --no-startup-id

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.


# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "increase_volume"
# bindsym XF86AudioLowerVolume exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "decrease_volume"
# bindsym XF86AudioMute exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "mute"
# bindsym XF86AudioMicMute exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "mic_mute"
bindsym XF86AudioPrev exec --no-startup-id playerctl previous
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause

# Brightness
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl sset 5%+
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl sset 5%-


bindsym XF86Go  exec ~/bin/headphones speak #F10
bindsym Cancel exec ~/bin/headphones # F11



# Screenshots
# bindsym Print exec --no-startup-id i3-maim -s

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec tilix
# bindsym $mod+Return exec 'env SHELL=/bin/xonsh terminology'
bindsym $mod+Return exec wezterm

# kill focused window
bindsym $mod+Shift+q kill

# Start dmenu (a program launcher)
bindsym ctrl+shift+space exec ulauncher-toggle
# bindsym $mod+shift+d exec "synapse -s"
# bindsym $mod+d exec "kupfer"
bindsym $mod+shift+t exec "~/bin/create-todo"
bindsym $mod+d exec "kupfer"
bindsym $mod+s exec "kupfer-exec ~/dotfiles/workspaces.kfcom"
bindsym $mod+w exec "kupfer-exec ~/dotfiles/firefox.kfcom"
# bindsym $mod+d exec "ulauncher-toggle"
bindsym Mod1+Shift+space exec 'albert toggle'
# unbind control+space
# bindsym $mod+space exec ulauncher-toggle
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed $mod+d exec --no-startup-id i3-dmenu-desktop

bindsym $mod+m move workspace to output left
# bindsym $mod+m exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev
bindsym $mod+grave workspace back_and_forth

# bindgesture swipe:right workspace prev
# bindgesture swipe:left workspace next




# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Mod1+h split h

# split in vertical orientation
bindsym $mod+Mod1+v split v

bindsym $mod+Mod1+t split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# group
bindsym $mod+g layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+u layout splitv
bindsym $mod+y layout splith

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

#  focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1:Write"
set $ws2 "2:RPG"
set $ws3 "3:PL"
set $ws4 "4:Theory"
set $ws5 "5:RPG2"
set $ws6 "6:RPG3"
set $ws7 "7:Tinker"
set $ws8 "8:PDFs"
set $ws9 "9:???"
set $ws10 "10:???"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10


# Resizing windows by 10 in i3 using keyboard only
bindsym $mod+Ctrl+Shift+Right resize shrink width 10 px or 10 ppt
bindsym $mod+Ctrl+Shift+Up resize grow height 10 px or 10 ppt
bindsym $mod+Ctrl+Shift+Down resize shrink height 10 px or 10 ppt
bindsym $mod+Ctrl+Shift+Left resize grow width 10 px or 10 ppt



# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize shrink height 10 px or 10 ppt
        bindsym Up resize grow height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"


# Make mouse work, hopefully
# exec_always xrandr --output $(xrandr | grep -m 1 XWAYLAND | awk '{print $1;}') --primary
# exec_always xrandr --output HDMI-A-1 --primary



# Autostarts
# exec --no-startup-id "/usr/lib/geoclue-2.0/demos/agent"
exec --no-startup-id redshift-gtk
exec_always kdeconnectd
exec_always playerctld
# exec --no-startup-id "~/bin/autosuspend"
# exec_always --no-startup-id "sudo updatedb"
exec_always --no-startup-id "~/bin/laptop.sh"
# output "*" bg "/home/vivien/backgrounds/nebulae-sky" fill
# output "*" bg "/home/vivien/backgrounds/panopticon.jpg" fill
# output "*" bg "/home/vivien/backgrounds/dracula-wallpaper/second-collection/waves/dracula-waves-44475a.png" fill
# exec --no-startup-id "feh --bg-scale /home/vivien/Pictures/backgrounds/dracula-wallpaper/second-collection/waves/dracula-waves-44475a.png"
# exec --no-startup-id "feh --bg-scale ~/Pictures/backgrounds/rosepine/rose_pine_circle.png"
# exec --no-startup-id "feh --bg-scale ~/Pictures/backgrounds/morgana.jpg"
exec --no-startup-id "feh --bg-scale /home/vivien/Pictures/backgrounds/flondon/foglet_harbor.jpg"
# output "*" bg "/home/vivien/backgrounds/squid" fill
exec --no-startup-id nm-applet
exec volctl
# exec_always --no-startup-id autotiling-rs
exec_always --no-startup-id autotiling
exec_always libinput-gestures -c ~/dotfiles/libinput-gestures.conf
# exec_always "ulauncher --no-window-shadow --hide-window"
# exec --no-startup-id "onedrive --monitor --confdir ~/.config/onedrivePersonal"
# exec --no-startup-id "compton -cb --config ~/.config/picom/picom.conf"
# exec 'env SHELL=/bin/xonsh terminology'
# exec $BROWSER
exec $TERMINAL
exec "killall -9 emacs"
exec "trash ~/.config/emacs/.cache/.emacs.desktop.lock"
exec "~/.screenlayout/dubai-monitor.sh"
# exec_always --no-startup-id "cd /home/vivien/apps/5e-srd-api && docker compose up --restart always" # Run the 5e API on startup
exec --no-startup-id "cd /home/vivien/apps/pihole-local/ && docker compose up -d"


exec "synapse -s &"

# Window focus
#bindsym $mod+v [class="Vivaldi-stable"] focus
# bindsym $mod+Shift+v exec nextmatch Firefox
bindsym $mod+v exec vivaldi
# bindsym $mod+e exec emacsclient -nc --eval '(progn (doom/switch-to-scratch-buffer) (consult-buffer))'
# bindsym $mod+e exec ~/bin/eclient
bindsym $mod+e exec emacsclient -nc --eval "(doom/switch-to-scratch-buffer)"
bindsym $mod+Shift+x exec emacsclient -nc --eval "(doom/switch-to-scratch-buffer)"
bindsym $mod+x exec 'emacsclient --eval "(emacs-everywhere)"'
bindsym $mod+Shift+e exec emacsclient  -nc --eval "(deft-new-file)"
# bindsym $mod+\/ exec com.github.alainm23.planner.quick-add
# bindsym $mod+n exec nextmatch Org.gnome.Nautilus
# bindsym $mod+Shift+n exec emacsclient -nc --eval '(ranger "/home/rohan/drive/School")'
bindsym $mod+Shift+n exec nemo
# scratchpad handling
#for_window [class="Mailspring"] move window to scratchpad

#for_window [class="Org.gnome.Nautilus"] move window to scratchpad

#for_window [class="Tilix"] move window to scratchpad
# 1900x1200
bindsym $mod+Shift+minus floating enable, resize set width 1728 height 1088, move position center, move scratchpad
bindsym $mod+minus scratchpad show

#bindsym $mod+0 [class="Mailspring"] scratchpad show
# bindsym Mod1+3 [class="Tilix"] scratchpad show
bindsym Mod1+3 [class="org.wezfurlong.wezterm"] scratchpad show

# for_window [title="Emacs Everywhere*"] move scratchpad, move position absolute center scratchpad show
# for_window [title="scratchmacs"] move scratchpad, move position absolute center, scratchpad show
# bindsym $mod+3 [class="Tilix"] scratchpad show
bindsym $mod+Shift+s [class="tidal-hifi"] scratchpad show
bindsym $mod+Shift+d [class="discord"] scratchpad show
bindsym $mod+n [class="Emacs"] scratchpad show
bindsym $mod+b [class="Vivaldi-stable"] scratchpad show
# bindsym $mod+Shift+d [class="Vivaldi-stable" instance="discord.com__login"] scratchpad show


for_window [instance="spotify"] sticky enable
for_window [title="fzf-switcher"] floating enable
for_window [title="Extension: (Quick Commands) - Quick Commands — Mozilla Firefox"] floating enable
# for_window [title="Extension: (Quick Commands) - Quick Commands — Mozilla Firefox"] sticky enable

# SwayFX Aesthetics

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# exec_always --no-startup-id ~/apps/i3scripts/bar.sh

# Rules, because teams sucks
# for_window [class="^.*"] border pixel 4
for_window [title="Microsoft Teams Notification"] floating enable
no_focus [title="Microsoft Teams Notification"]
for_window [class="Pavucontrol"] floating enable
for_window [class="Synapse"] border pixel 0
for_window [floating] border pixel 0
# for window [title="Grade 9A"] floating enable


## Lock Screen
bindsym $mod+F1 exec loginctl lock-session


    # bindsym print exec '/usr/share/swayfx/scripts/grimshot --notify save area'
bindsym Print exec --no-startup-id "screengrab -r"

    # bindsym Shift+print exec ~/.config/sway/scripts/screenshot.sh

# set $laptop eDP-1
# bindswitch --reload --locked lid:on output $laptop disable
# bindswitch --reload --locked lid:off output $laptop enable

# exec_always ~/.config/sway/scripts/clamshell.sh
## Autostarts
# Auth with polkit-gnome:
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Desktop notifications
exec_always dunst
exec_always dunstctl set-paused true # Shut up notifications

# Network Applet
exec nm-applet --indicator

# GTK3 applications take a long time to start
# exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
# exec hash dbus-update-activation-environment 2>/dev/null && \
#      dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK

# Welcome App
# exec dex -a -s /etc/xdg/autostart/:~/.config/autostart/

# Sway Fader
# exec python3 ~/.config/sway/scripts/swayfader.py


exec "killall teams"
exec "killall xfce4-notifyd"

for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [window_role="About"] floating enable
for_window [title="File Operation Progress"] floating enable, border pixel 3, sticky enable, resize set width 40 ppt height 30 ppt
for_window [class="firefoxdeveloperedition" title="^Library$"] floating enable, border pixel 1, sticky enable, resize set width 40 ppt height 30 ppt
for_window [class="floating_shell_portrait"] floating enable, border pixel 1, sticky enable, resize set width 30 ppt height 40 ppt
for_window [title="Picture in picture"] floating enable, sticky enable
# for_window [title="nmtui"] floating enable,  resize set width 50 ppt height 70 ppt
# for_window [title="htop"] floating enable, resize set width 50 ppt height 70 ppt
for_window [class="xsensors"] floating enable
for_window [title="Save File"] floating enable
for_window [class="firefoxdeveloperedition" title="Firefox — Sharing Indicator"] kill

# Inhibit idle
for_window [class="firefoxdeveloperedition"] inhibit_idle fullscreen
for_window [class="Chromium"] inhibit_idle fullscreen


## New Coloring (Endeavor)
# class                 border  bground text    indicator child_border

# TMA
# shadow_color #9fcc2e
# client.focused          #9fcc2e #9fcc2e #F8F8F2 #9fcc2e   #9fcc2e


set $rosewater #f5e0dc
set $flamingo #f2cdcd
set $pink #f5c2e7
set $mauve #cba6f7
set $red #f38ba8
set $maroon #eba0ac
set $peach #fab387
set $yellow #f9e2af
set $green #a6e3a1
set $teal #94e2d5
set $sky #89dceb
set $sapphire #74c7ec
set $blue #89b4fa
set $lavender #b4befe
set $text #cdd6f4
set $subtext1 #bac2de
set $subtext0 #a6adc8
set $overlay2 #9399b2
set $overlay1 #7f849c
set $overlay0 #6c7086
set $surface2 #585b70
set $surface1 #45475a
set $surface0 #313244
set $base #1e1e2e
set $mantle #181825
set $crust #11111b

# target                 title     bg    text   indicator  border
client.focused           $lavender $base $text  $rosewater $lavender
client.focused_inactive  $overlay0 $base $text  $rosewater $overlay0
client.unfocused         $overlay0 $base $text  $rosewater $overlay0
client.urgent            $peach    $base $peach $overlay0  $peach
client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
client.background        $base

# bar
bar {

       status_command i3blocks
       i3bar_command i3bar --transparency
	   font pango:RobotoSlab, FontAwesome5Free, FontAwesome5Brands, SymbolsNerdFont 10
       position top
       height 50
       mode hide
       modifier Mod4
       tray_padding 10px
       # separator_symbol "〉〉〉"
       separator_symbol ""
  colors {
    background         $base
    statusline         $text
    focused_statusline $text
    focused_separator  $base

    # target           border bg        text
    focused_workspace  $base  $mauve    $crust
    active_workspace   $base  $surface2 $text
    inactive_workspace $base  $base     $text
    urgent_workspace   $base  $red      $crust
  }
}

#
# Keybindings List
#
    # bindsym $mod+t exec xed ~/.config/sway/cheatsheet


#bar {
#        status_command i3status
#        position top
#        height 25
#        mode dock
#}

# KDE config
# Plasma compatibility improvements
# for_window [window_role="pop-up"] floating enable
# for_window [window_role="task_dialog"] floating enable

# for_window [class="yakuake"] floating enable
# for_window [class="systemsettings"] floating enable
# for_window [class="plasmashell"] floating enable;
# for_window [class="Plasma"] floating enable; border none
# for_window [title="plasma-desktop"] floating enable; border none
# for_window [title="win7"] floating enable; border none
# for_window [class="krunner"] floating enable; border none; move position center
# for_window [class="Kmix"] floating enable; border none
# for_window [class="Klipper"] floating enable; border none
# for_window [class="Plasmoidviewer"] floating enable; border none
# for_window [class="(?i)*nextcloud*"] floating disable
# for_window [class="plasmashell" window_type="notification"] border none, move right 700px, move down 450px
# no_focus [class="plasmashell" window_type="notification"]
for_window [title="Extension: (Quick Commands) - Quick Commands — Mozilla Firefox"] floating enable
for_window [class="Kupfer.py"] floating enable
for_window [class="Kupfer.py"] move position center
# for_window [title="Desktop @ QRect.*"] kill; floating enable; border none
# for_window [title="Desktop — Plasma"] kill, floating enable, border none


## Audio
# bindsym XF86AudioRaiseVolume exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "increase_volume"
# bindsym XF86AudioLowerVolume exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "decrease_volume"
# bindsym XF86AudioMute exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "mute"
# bindsym XF86AudioMicMute exec --no-startup-id qdbus org.kde.kglobalaccel /component/kmix invokeShortcut "mic_mute"
# exec_always setxkbmap -option parens:swap_brackets -option caps:ctrl_modifier -option terminate:ctrl_alt_bksp
# exec_always xcape -e 'Caps_Lock=Escape'
# exec_always xmodmap ~/.xmodmap
